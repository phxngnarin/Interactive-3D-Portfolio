/**
 * ============================================================
 *  config.js  –  แก้ไขเนื้อหา / รูป / สี ของเว็บได้ที่ไฟล์นี้ไฟล์เดียว
 * ============================================================
 *
 *  วิธีเปลี่ยนรูป:
 *    1. เอาไฟล์รูปไปวางในโฟลเดอร์  assets/images/  (จะสร้างโฟลเดอร์ย่อยเองก็ได้)
 *    2. แก้ path ด้านล่างให้ชี้ไปที่ไฟล์นั้น  เช่น  "assets/images/about/me.jpg"
 *       (path นับจากโฟลเดอร์ที่มี index.html)
 *    - ใช้ลิงก์รูปจากเว็บ (https://...) ก็ได้
 *    - รูปจะถูกครอปให้เต็มกรอบอัตโนมัติ (แบบ object-fit: cover) ขนาดไหนก็ใช้ได้
 *
 *  ถ้าไฟล์รูปหาไม่เจอ ระบบจะแสดงรูปเดิมในโมเดลแทน (ไม่ทำให้เว็บพัง)
 */

export const config = {
  /* ---------------------------------------------------------- */
  /*  ข้อมูลทั่วไป                                                */
  /* ---------------------------------------------------------- */
  site: {
    title: "PHONGNARIN's Room",
    // แสดงเมื่อแชร์ลิงก์ (og:description)
    description: "PHONGNARIN's 3D portfolio room — welcome!",
  },

  loading: {
    greeting: "ยินดีต้อนรับ", // ข้อความบนหน้าจอโหลดหลังกดเข้า
    enter: "กดเลย!",
    enterWithoutSound: "กดปิดเสียงได้ :(",
  },

  // ชื่อบนป้ายไม้ข้างหน้าต่าง (ตัวอักษรจะถูกสร้างเป็น 3D ให้อัตโนมัติ – เปลี่ยนชื่อได้เลย)
  // แนะนำ A-Z / 0-9 / เว้นวรรค (ฟอนต์ 3D รองรับภาษาอังกฤษเท่านั้น)
  signName: "PHONGNARIN",

  /* ---------------------------------------------------------- */
  /*  กรอบรูปถ่ายบนโต๊ะข้างคอมพิวเตอร์ (แทนที่การ์ดข้อมูลส่วนตัวเดิม)   */
  /* ---------------------------------------------------------- */
  deskPhoto: {
    // เอารูปตัวเอง/รูปที่ชอบไปวางที่ assets/images/profile/ แล้วใส่ path ตรงนี้
    // เช่น "assets/images/profile/me.jpg" (จะครอปให้เต็มกรอบอัตโนมัติ)
    // ปล่อยเป็น null ไว้ก่อนได้ — จะโชว์เป็นกรอบเปล่า ไม่ทำให้เว็บพัง
    image: "assets/images/profile/me.jpg",
  },

  /* ---------------------------------------------------------- */
  /*  โทนสีห้อง                                                    */
  /* ---------------------------------------------------------- */
  // ห้องต้นฉบับเป็นโทนม่วง/ชมพู  ตอนนี้ตั้งเป็น "ฟ้าอ่อน"  ตรงนี้คือการ "ย้ายสีม่วง-ชมพู" ไปเป็นสีอื่น
  // โดยสีไม้ / สีเขียว / น้ำ จะไม่ถูกเปลี่ยน
  //
  //   targetHue (0-360) : สีใหม่  →  203 = ฟ้าอ่อน (sky blue)  ← ค่าเริ่มต้น
  //                                   172 = เขียวมิ้นต์, 140 = เขียว, 22 = ส้มพีช, 330 = ชมพูเข้ม, 260 = ม่วง(ต้นฉบับ)
  //   spread  (0-1)     : 1 = คงความต่างระหว่างสีม่วง/ชมพูเดิมไว้, 0 = ให้เป็นสีเดียวกันหมด
  //   saturation        : ความสด  (1 = เท่าเดิม)
  //   brightness        : ความสว่าง (1 = เท่าเดิม)
  //
  // ลองค่าสำเร็จรูปได้ที่ ROOM_PRESETS ด้านล่าง เช่น  room: ROOM_PRESETS.peach
  room: {
    enabled: true,
    sourceHue: 305,
    sourceRange: 80,
    targetHue: 203,
    spread: 0.45,
    saturation: 1.05,
    brightness: 1.03,
  },

  // สีพื้นหลังฉาก (ด้านหลังห้อง)
  sceneBackground: "#D3E6F3",

  // สีของปุ่ม / กล่องข้อความ (UI) – โหมดกลางวัน / กลางคืน
  ui: {
    light: {
      dark: "#245f8f", // สีตัวอักษรหัวข้อ
      main: "#4a8fc4", // สีขอบ / ปุ่ม
      soft: "#b3d7f0",
      bg: "#e2f1fb", // พื้นกล่องข้อความ
      text: "#0c2a40",
    },
    dark: {
      dark: "#d3e9f8",
      main: "#7fbbe6",
      soft: "#1f4d73",
      bg: "#0f2f4a",
      text: "#e2f1fb",
    },
  },

  /* ---------------------------------------------------------- */
  /*  MY WORK  –  กดป้าย "My Work" แล้วกล้องซูมไปที่หน้าจอคอม        */
  /* ---------------------------------------------------------- */
  work: {
    // วิดีโอที่เล่นบนหน้าจอตอนไม่ได้ดูผลงาน (ใส่ null = ใช้รูป/สีพื้นแทน)
    idleVideo: "assets/textures/video/Screen.mp4",
    // รูปถ่ายตัวเอง (นิ่ง) ที่จะโชว์บนจอคอมพิวเตอร์แทนวิดีโอ (ตรงเงื่อนไข "ใส่รูปถ่ายตัวเอง (texture)")
    // เอาไฟล์รูปไปวางที่ assets/images/profile/ แล้วใส่ path ตรงนี้ เช่น "assets/images/profile/me.jpg"
    // ถ้าใส่ path ไว้ตรงนี้ จะโชว์รูปนี้แทนวิดีโอด้านบนตอนหน้าจอว่าง (ปล่อยเป็น null ถ้ายังไม่มีรูป จะใช้วิดีโอแทน)
    idleImage: null,
    // ระยะกล้องตอนกด "My Work" แล้วซูมไปที่จอคอม
    //   0     = ระยะเดิม (ค่าเริ่มต้น)
    //   บวก   = ถอยกล้องออกจากจอเพิ่ม (เห็นจอเล็กลง แต่เห็นโต๊ะ/บริเวณรอบ ๆ กว้างขึ้น ไม่โดนขอบตัด)
    //   ลบ    = ขยับกล้องเข้าใกล้จอมากขึ้น
    // ปรับทีละ 0.2-0.5 แล้วลองดูผลก่อน (หน่วยเดียวกับพิกัดฉาก ก่อนย่อขนาดห้อง)
    cameraPullBack: 0.8,
    // เพิ่ม/ลด/สลับลำดับโปรเจกต์ได้ตามต้องการ (กี่อันก็ได้)
    projects: [
      {
        title: "Project 1",
        image: "assets/images/work/work-1.jpg",
        description:
          "ใส่คำอธิบายผลงานของคุณตรงนี้ — แก้ได้ที่ไฟล์ src/config.js (ส่วน work.projects)",
        skills: ["Skill A", "Skill B", "Skill C"],
        link: "", // ใส่ลิงก์ผลงานได้ (เว้นว่าง = ไม่แสดงปุ่ม)
      },
      {
        title: "Project 2",
        image: "assets/images/work/work-2.jpg",
        description:
          "รายละเอียดโปรเจกต์ที่สอง — เล่าว่าทำอะไร ใช้เทคโนโลยีอะไร ได้ผลลัพธ์ยังไง",
        skills: ["Skill D", "Skill E"],
        link: "",
      },
      {
        title: "Project 3",
        image: "assets/images/work/work-3.jpg",
        description: "รายละเอียดโปรเจกต์ที่สาม",
        skills: ["Skill F", "Skill G", "Skill H"],
        link: "",
      },
    ],
  },

  /* ---------------------------------------------------------- */
  /*  ABOUT  –  กดป้าย "About" แล้วกล้องไปที่ป้ายไม้ข้อมูล 3 อันข้างโต๊ะ */
  /*  (เดิมเป็นรูปถ่าย 3 รูป ตอนนี้เปลี่ยนเป็นป้ายไม้ + ตัวอักษร 3D       */
  /*  จริง ๆ จึงพิมพ์ได้เฉพาะภาษาอังกฤษ/ตัวเลข — title = หัวข้อ,        */
  /*  lines = รายละเอียดแต่ละบรรทัด เช่น สกิล)                        */
  /* ---------------------------------------------------------- */
  // frames[0] = กรอบบนซ้าย, frames[1] = กรอบบนขวา, frames[2] = กรอบล่าง (ตามที่มองจากมุมเริ่มต้น)
  // ถ้าไม่ใช่ตามนี้ ลองสลับลำดับดูได้
  about: {
    intro: "Click a frame to see the details",
    frames: [
      {
        title: "SKILLS",
        lines: ["JavaScript & Node.js", "Lua", "Three.js / WebGL", "Discord.js"],
      },
      {
        title: "PROJECTS",
        lines: ["Web App Prototypes", "Custom Discord Bots"],
      },
      {
        title: "FOCUS",
        lines: ["Interactive 3D Web", "Game System Design", "Thai-Language UI/UX"],
      },
    ],
  },

  /* ---------------------------------------------------------- */
  /*  CONTACT  –  กดป้าย "Contact" แล้วกล้องไปที่ป้ายไม้ข้อมูล 3D อันเดียว */
  /*  (เดิมเป็นไอคอน GitHub / Facebook / Discord ตอนนี้เปลี่ยนเป็นป้ายไม้   */
  /*  สลักข้อความ 3D จริง ๆ จึงพิมพ์ได้เฉพาะภาษาอังกฤษ/ตัวเลขเท่านั้น)      */
  /* ---------------------------------------------------------- */
  contact: {
    title: "Contact",
    text: "",
    sign: {
      name: "Phongnarin Ratanachatvichien",
      studentId: "6721651556",
      // แก้บรรทัดรายละเอียดเพิ่มเติมได้ตรงนี้ (กี่บรรทัดก็ได้)
      lines: ["Computer Science", "Faculty of Science and Arts", "Kasetsart University"],
      // ปรับตำแหน่งป้ายไม้ข้อมูลติดต่อ 3D นี้ได้ตรงนี้ (ไม่ต้องแก้โค้ด)
      //   y บวก = ยกป้ายขึ้นสูงขึ้น, y ลบ = เลื่อนป้ายลงต่ำลง
      //   x / z ใช้เลื่อนป้ายซ้าย-ขวา / หน้า-หลัง เพิ่มเติมได้เช่นกัน (ปกติไม่ต้องแก้)
      offset: { x: 0, y: 0.5, z: 0 },
    },
  },
};

/* ------------------------------------------------------------ */
/*  โทนสีห้องสำเร็จรูป – ก็อปไปวางที่ config.room ได้เลย            */
/* ------------------------------------------------------------ */
export const ROOM_PRESETS = {
  original: { enabled: false },
  sky: { enabled: true, sourceHue: 305, sourceRange: 80, targetHue: 203, spread: 0.45, saturation: 1.05, brightness: 1.03 },
  mint: { enabled: true, sourceHue: 305, sourceRange: 80, targetHue: 172, spread: 0.55, saturation: 1.15, brightness: 1.0 },
  peach: { enabled: true, sourceHue: 305, sourceRange: 80, targetHue: 22, spread: 0.4, saturation: 1.0, brightness: 1.05 },
  lime: { enabled: true, sourceHue: 305, sourceRange: 80, targetHue: 105, spread: 0.5, saturation: 1.1, brightness: 1.0 },
};
